# 프로젝트 에이전트 규칙

## Git 커밋 & 푸시 규칙

작업(코딩, 파일 수정, 프로젝트 변경 등)을 완료하고 멈추기 전에 반드시 다음을 수행해야 한다:

1. `git add .` -- 변경사항을 스테이징
2. `git commit -m "작업 내용 요약"` -- 의미 있는 커밋 메시지로 커밋
3. `git push` -- 원격 저장소에 푸시

이 규칙은 예외 없이 모든 작업 종료 시점에 적용된다.
Git 저장소가 없는 경우에는 예외로 한다.
